import { SYMBOL } from '../../../utils/symbols';
import ItemContainer from '../ItemContainer';

export default class Trashcan extends ItemContainer {
  static NAME = 'Trashcan';
  static SYMBOL = SYMBOL.TRASHCAN;
}
