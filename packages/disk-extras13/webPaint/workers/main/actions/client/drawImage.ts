// import type { Context } from '../../../../types/main';
// import type { ActionCommandToMainWorker } from '../../../../types/worker.message.main';
// import type { DrawImagePayload } from '../../../../types/worker.payload';

/**
 * @deprecated Not implemented yet.
 */
export default function loadImage() {
  //   { ctx, updateDisplays }: Context,
  //   data: ActionCommandToMainWorker<DrawImagePayload>
  // ) {
  //   if (ctx) {
  //     const { imageBitmap, x, y, width, height } = data.payload;
  //     ctx.drawImage(imageBitmap, x, y, width, height);
  //     imageBitmap.close();
  //     updateDisplays();
  //   }
}
